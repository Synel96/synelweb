export default {
  // Enable static site generation (pre-rendering)
  prerender: true,
  
  // Client routing for SPA behavior
  clientRouting: true,
  
  // Hydration for interactive components
  hydrationCanBeAborted: true
};
